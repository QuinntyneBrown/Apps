@startuml
class Recommendation {
    +Guid Id
    +Guid UserId
    +Guid ScreeningTypeId
    +string ReasonCode
    +Date DueDate
    +int Priority
    +Generate()
    +Customize()
}

class RecommendationEngine {
    +CalculateRecommendations(user)
    +ApplyRiskFactors(user, recs)
    +CheckAgeThresholds(user)
}

class ClinicalGuideline {
    +Guid Id
    +int MinAge
    +int MaxAge
    +string Gender
    +int IntervalMonths
    +string Source
}

RecommendationEngine --> Recommendation
RecommendationEngine --> ClinicalGuideline
@enduml
