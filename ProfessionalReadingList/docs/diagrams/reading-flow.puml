@startuml ReadingFlow

start

:User discovers content;

:Add to reading list;

:System fetches metadata;

if (Content type?) then (Article)
  :Extract from URL;
else (Book)
  :ISBN lookup;
endif

:Categorize and tag;

:Add to queue;

:User starts reading;

:Track reading session;

while (Reading in progress?)
  :Update progress;
  if (Take notes?) then (yes)
    :Create highlights;
    :Add notes;
    :Capture insights;
  endif
endwhile

if (Completed?) then (yes)
  :Mark as completed;
  :Update goals;
  :Trigger milestones;
else (abandoned)
  :Mark as abandoned;
  :Record reason;
endif

:Review notes and insights;

if (Share?) then (yes)
  :Recommend to colleagues;
  :Add to shared list;
endif

stop

@enduml
