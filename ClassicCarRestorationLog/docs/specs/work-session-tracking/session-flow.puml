@startuml Work Session Flow

|User|
start
:Prepare workspace;
:Start work session;

|System|
:Create session record;
:Start timer;

|User|
while (Work remaining?) is (yes)
    :Complete task;
    |System|
    :Log task completion;
    |User|
    if (Problem discovered?) then (yes)
        :Report problem;
        |System|
        :Create problem record;
        :Notify user;
    endif
endwhile (no)

:End work session;

|System|
:Stop timer;
:Calculate duration;
:Update project progress;
:Publish WorkSessionCompleted event;

stop

@enduml
